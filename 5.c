/*Write a program to accept a number as input (Octal or Hexadecimal) from the user and print it decimal equivalent. 
Input Format: A Single Integer 'num' - denoting either a hexadecimal or octal value Output Format: Display the Integer in decimal format*/
#include<stdio.h>
int main()
{
    int a;
    scanf("%i",&a);
    printf("%d",a);
}
